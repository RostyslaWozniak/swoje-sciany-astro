---
import { cn } from 'src/utils/cn';

type Props = {
  className?: string;
};

const { className } = Astro.props;
---

<div
  class:list={cn(
    'mx-auto h-full max-w-[1300px]  px-2.5 py-14 sm:px-5 sm:py-16 lg:py-20',
    className
  )}>
  <slot />
</div>
