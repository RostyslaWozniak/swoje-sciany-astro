---
import { cn } from "src/utils/cn";

type Props = {
  className?: string;
};

const { className } = Astro.props;
---

<div
  class:list={cn(
    "mx-auto h-full max-w-[1400px]  px-4 py-14 sm:px-5 sm:py-16 lg:py-24",
    className
  )}
>
  <slot />
</div>
