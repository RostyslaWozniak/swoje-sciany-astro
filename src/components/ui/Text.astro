---
import { cn } from "@utils/cn";

type Props = {
  className?: string;
};

const { className } = Astro.props as Props;
---

<p class:list={cn("text-center text-sm md:text-base", className)}>
  <slot />
</p>
