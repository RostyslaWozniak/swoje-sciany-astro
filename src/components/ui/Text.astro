---
import { cn } from "@utils/cn";

type Props = {
  className?: string;
};

const { className } = Astro.props as Props;
---

<p class:list={cn("sm:text-md text-center", className)}>
  <slot />
</p>
